import { MarkedMapPipe } from './marked-map.pipe';

describe('MarkedMapPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkedMapPipe();
    expect(pipe).toBeTruthy();
  });
});
